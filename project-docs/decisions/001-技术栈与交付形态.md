# 001 · 技术栈与交付形态（含收编决策）

> **等级**：system（历史档案，**只增不改**）｜ 被取代 = 新增文件标注，不改本文

## 决策内容

- 将临时文件夹中的 `Layout_assistant`（微信公众号排版助手）收编为正式项目，保留其既有技术栈与 git 历史：**原生 JS 无构建链 + CDN 第三方库 + 纯静态双形态**（拆分版主形态 + `single-file.html` 单文件分发形态）。

## 背景与备选

- **当时的问题**：项目在临时文件夹中待归拢，且自带 .git（origin 已推送 GitHub）、工作区有未提交的精简改动；需决定收编方式与 git 处理。
- **备选方案**：
  - A 原样归档不立项——只收纳不动体系，总览登记一行；代价：无宪法/验证基建，后续维护散
  - B 立项收编（**选定**）——先 commit 固化工作区状态，整包搬入顶层 `layout-assistant/`，保留 .git 与远端，补齐 project-docs + 验证基建
  - C 剥离 .git 只留文件快照——最干净，但丢本地历史与分支（main-backup-old / publish）
- **选择理由**：用户拍板"立项为正式项目、后面都由 AI 负责、两形态都可重新维护"；git 历史已推 GitHub 有远端备份，固化 commit 后整包搬移数据最安全（被删文件仍可从历史找回）。

## 知识层接线（BP 库命中记录）

- **BP-001**（验证脚本首跑失败→先读完整输出定位根因）：采用——verify.sh 首跑人工看完整输出；失败先分依赖/网络/权限，不先怀疑文件缺失。
- **BP-017**（"写完了"≠"做对了"，完成标准=验证通过）：采用——宪法 §3 质量纪律明确"每任务自验、全绿才算完成"，verify.sh 全部输出可自动判定（exit code）。

## 影响

- 影响的模块/约束/红线：宪法 §1 技术栈红线（无构建链、CDN 白名单）；§7 编码规范（XSS/CSP/缓存戳纪律）；交付形态锁定"纯静态 Web 工具"，引入服务端/打包器 = 架构变更须新 decision。

## 状态

- ✅ 生效（2026-09-16 用户拍板立项）
